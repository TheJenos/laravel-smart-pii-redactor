name: run-tests

on:
  push:
    paths:
      - '**.php'
      - '.github/workflows/run-tests.yml'
      - 'phpunit.xml.dist'
      - 'composer.json'
      - 'composer.lock'
  pull_request:
    paths:
      - '**.php'
      - '.github/workflows/run-tests.yml'
      - 'phpunit.xml.dist'
      - 'composer.json'
      - 'composer.lock'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest

    name: PHP 8.5 - Laravel 13.* - prefer-lowest - ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.5
          extensions: dom, curl, libxml, mbstring, zip, bz2, pcntl, pdo, sqlite, pdo_sqlite, bcmath, soap, intl, gd, exif, iconv, imagick, fileinfo
          coverage: none

      - name: Setup problem matchers
        run: |
          echo "::add-matcher::${{ runner.tool_cache }}/php.json"
          echo "::add-matcher::${{ runner.tool_cache }}/phpunit.json"

      - name: Install dependencies
        run: |
          composer require "laravel/framework:13.*" "orchestra/testbench:11.*" --no-interaction --no-update
          composer update --prefer-lowest --prefer-dist --no-interaction

      - name: List Installed Dependencies
        run: composer show -D

      # Mitie's own post-install hook only runs when it's the root package, so the native library is fetched here...
      - name: Install MITIE native library
        run: php -r "require 'vendor/autoload.php'; Mitie\Vendor::check();"

      - name: Cache NER model
        uses: actions/cache@v4
        with:
          path: src/Models/ner_model.dat
          key: mitie-ner-model-v0.2

      - name: Download NER model
        run: vendor/bin/testbench smart-pii-redactor:init

      - name: Execute tests
        run: vendor/bin/pest --ci
